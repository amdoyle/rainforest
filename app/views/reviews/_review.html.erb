
  <p>Reviews for <%= @product.name %></p>
    <li><%= @review.comment %></li>
      <p   class="poster">
        <% if @review.user != nil %>
          <em> by <%= @review.user.name %> </em>
          Added on: <%= @review.created_at %>
        <% end %>
    </p >
